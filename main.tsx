import React from "react";
import ReactDOM from "react-dom/client";
import CounterTimer from "./App";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <CounterTimer />
  </React.StrictMode>
);
